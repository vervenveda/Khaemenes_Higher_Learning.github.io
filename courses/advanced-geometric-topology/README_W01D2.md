# W01D2 Upload Instructions

Add:
- curriculum/unit-01/week-01/w01d2.js
- teacher-key/unit-01/week-01/W01D2_KEY.md

Merge the contents of `assessments-week-01-addition.js` into the existing:
- assessments/formative/week-01.js
before its final `})();`, OR replace that file with a combined version.

Then update `lesson.html` by adding:
`<script src="curriculum/unit-01/week-01/w01d2.js"></script>`
after the W01D1 module line.

The existing `assessments/formative/week-01.js` script line does not change.

GitHub direct write was attempted but the integration returned HTTP 403, so this package is provided for the same manual upload workflow used successfully for W01D1.
